# REQUEST

## 프로젝트 제목 (확정)

| # | 프로젝트 제목 (한글) | 폴더명 (영문 kebab-case) | 강조 포인트 |
|---|---|---|---|
| 1 | 업무 워크플로우 허브 | `workflow-hub` | 워크플로우 형식화·공유·인수인계·매뉴얼 보관소를 모두 포괄 |
| 2 | 코드 인수인계 허브 | `code-handover-hub` | 인수인계 불가 문제를 정면으로 반영 |
| 3 | 부서 코드 레지스트리 | `dept-code-registry` | 코드/매뉴얼 저장소 성격 강조 |
| 4 | 요청 보드 & 코드 위키 | `request-board-wiki` | 요청 트래킹 + 위키 두 축을 함께 명시 |
| 5 | 코드 자산 관리 시스템 | `code-asset-manager` | 버전 등록·자산 관리 관점 |
| 6 | 팀 코드 아카이브 | `team-code-archive` | 담백하게 저장/보관 중심 |
| 7 | 업무 인계 트래커 | `handoff-tracker` | 업무(요청) 흐름 추적에 방점 |
| 8 | 코드 & 매뉴얼 라이브러리 | `code-manual-library` | 코드+매뉴얼 쌍을 명확히 드러냄 |

> **1번 「업무 워크플로우 허브」 / `workflow-hub`** 로 확정 (아래 「추가 요청사항」 참조).

## 사용자 요청사항 (원문)

부서원 4명 정도입니다. 개인은 각각 독립적으로 코드를 작성하고 코드 공유가 되고 있지 않습니다. 업무는 코드 / 프로그램 요청이 옵니다. 작성한 코드 및 메뉴얼은 공유가 되지 않기 때문에 다른 사람이 대신 하거나 수정할 수 없습니다.

요청 프로그램 리스트 관리 (리스트 / 진행중 / 완료). 각 개인은 자신이 맡은 코드 개발 업무를 등록하고, 완료시 코드와 매뉴얼을 버젼 등록 / 등록합니다. 다른 사람은 이전에 추가 수정 요청이 올경우, 다른 사람이 개발한 코드와 매뉴얼을 참고로 반영하고 새로운 버젼을 등록합니다.

코드 유지 보수 / 업무 인수인계 / 부서내 메뉴얼 관리 / 필요시 개발 코드 매뉴얼에 대한 LLM wiki 구성 등입니다.

업무 워크플로우 형식화 및 업무공유 / 업무 인수 인계 / 업무 메뉴얼 보관소 등이 최종 목표입니다.

## 추가 요청사항 (기획 대화 중 확정)

### 프로젝트 제목
위 후보 중 **1번 「업무 워크플로우 허브」 / `workflow-hub`** 로 확정.

### backlog MCP — 이 프로젝트 전용으로 재구성

day2 `backlog-cli-lab`과의 **연관성을 제거**하고, 이 폴더 전용으로 backlog MCP를 다시 구성한다.

현재 상태 (확인 완료):

- `backlog` 서버는 `~/.claude.json`의 **user 스코프**에 등록돼 있고,
  `node D:\projects\vibe-coding-claude\day2\backlog-cli-lab\mcp\server.mjs` 절대경로를 가리킨다.
  그래서 이 프로젝트에서도 이미 보이지만, **소유자는 day2**다.
- `mcp/server.mjs`의 기본 파일 경로가 **서버 파일 위치 기준으로 계산**된다
  (`projectRoot = <server.mjs>/..`, `defaultFilePath = projectRoot/backlog.json`).
  따라서 인자 없이 호출하면 **day2의 `backlog.json`을 읽는다.**
- `file` 인자를 주면 임의 경로를 읽을 수 있으나, **빠뜨리면 조용히 day2를 읽는 것이 실제 위험**이다.
- 노출 도구는 `backlog_list`·`backlog_validate` **읽기 전용 2개**뿐이다.
  등록·상태 변경(쓰기) 도구는 없다.
- day2의 스키마는 재사용 가능하다:
  `id / status / priority / category / title / summary / where / parent / deps / doc / done_at / note`

재구성 방침:

- 이 폴더 안에 MCP 서버와 `backlog.json`을 두어, **`file` 인자 없이 호출해도 이 프로젝트를 보게** 만든다.
- day2 파일은 수정하지 않는다.
- 쓰기 경로는 **MCP가 아니라 CLI**로 간다 — day1 hook에서 확인된 설계다(아래 「hook 채택」 참조).
  MCP가 읽기 전용인 것은 결함이 아니라 의도된 분리이므로 그대로 둔다.

> 이 문서에는 방침만 기록한다. 실제 구성은 구현 단계에서 한다.

### 완료 판정 방식 — 3단 게이트

Phase 완료를 Claude가 스스로 통과시키지 않는다. 반드시 아래 순서를 거친다.

1. Claude가 완료 조건을 실행해 결과를 수집한다
2. **Codex로 적대적 교차 검증**한다 — Claude가 만든 것을 Claude가 판정하면 같은 착각을 반복한다
3. **통과했을 때만** 사람에게 보고하고 **승인을 요청**한다. 사람이 승인해야 다음 Phase로 넘어간다

**호출 방식 (확정)** — day-04 문서 `docs-v2/day-04-headless-security-review-and-coaching.md` 기준:

```
codex exec --sandbox read-only "<프롬프트>"      # 읽기 전용 — 검증 대상 파일을 고칠 수 없다
codex exec resume --last "<추가 질문>"           # 같은 세션으로 이어 묻기
```

- **설치 확인됨**: `codex-cli 0.147.0`
- 임무는 승인이 아니라 **반박**이다. 문서의 절차 셋을 그대로 쓴다:
  1. 1차 결론과 근거를 **위치(파일 경로·줄 번호·실행한 명령)**로 정리한다 — 설명이 아니라 위치여야 리뷰어가 원본을 직접 연다
  2. 읽기 전용으로 넘기고 **"반박하라"**고 지시한다 — 수정 수단이 없어야 지적만 할 수 있다
  3. **두 결론이 갈리는 지점만** 사람이 판단한다. 같게 말한 부분은 넘어간다
- 성공 기준(문서): 지적이 심각도로 분류돼 있고, 각 지적에 파일·줄 번호 근거가 붙어 있고, **검증 대상 파일이 하나도 수정되지 않았다**

### 샘플 데이터 — Claude 생성 후 대기

사내 실데이터·매뉴얼은 일절 쓰지 않는다.
**샘플 프로젝트 2개는 Claude가 생성한 뒤 대기하고**, 사람이 형식을 확인·승인해야 다음 Phase로 넘어간다.
(⑤의 Phase 1 승인 지점에 해당한다.)

### hook 채택 — day1·day2에서 확인한 것

day1 `backlog-cli-lab/.claude/hooks/block-backlog-json.mjs`,
day2 `backlog-cli-lab/.claude/hooks/`(3개)를 확인한 결과다.

| hook | 출처 | 채택 | 이유 |
|---|---|---|---|
| `backlog-json-guard` | day2 재사용 | **채택** | `backlog.json`은 SSOT이므로 직접 편집을 deny하고 쓰기는 CLI(`tools/backlog.mjs add\|update`)로만 통하게 한다 |
| `version-history-guard` | **신규 작성** | **채택** | 같은 패턴을 **버전 이력 파일**에 적용한다. ①의 문제가 이력 보존이라 이력 파일이 이 프로젝트의 SSOT이고, 덮어쓰기로 쌓인 이력이 날아가는 것을 막는다 |
| `test-verify-check` (Stop) | day2 재사용 | **채택 — Phase 1부터** | ④의 완료 조건을 전부 `npm test`로 옮기므로 Phase 1에서 이미 돌 것이 생긴다 |
| `destructive-command-guard` | day2 재사용 | **채택** (⑤ 확정 시 번복) | 되돌리기를 사람 승인에 맡기지 않고 기계가 막는다. 승인 지점을 Phase 종료 1회로 줄인 대가로 넣는다 |

day1 hook의 거부 메시지에서 설계 의도가 확인된다:

> `backlog.json is this project's SSOT and must not be read or edited directly with Read/Write/Edit.
> Use the project CLI instead: node tools/backlog.mjs [--file <path>] validate|list|show|add|update.`

즉 **읽기는 MCP, 쓰기는 CLI, 우회는 hook이 차단**하는 3단 구조다.

### 테스트 — A안 (④의 완료 조건을 그대로 옮김)

당초 **B안**(day2 테스트 재사용)으로 정했으나, ③ 범위 재정의로 **백로그 CLI·MCP가 제품에서 빠지면서
day2 테스트가 가리킬 대상이 사라져** 성립하지 않게 됐다. **A안으로 번복한다.**

- ④ Phase 0~3의 완료 조건을 그대로 `node --test` 파일로 옮긴다.
- `npm test` = `node --test` (day2와 동일).
- 결과: 완료 판정과 검증 게이트가 **같은 것**이 된다. 테스트로 옮길 수 없는 조건은 애초에 기계 판정이 안 되는 조건이다.

> ⚠️ **Stop hook 선행 조건**
>
> `test-verify-check.mjs`는 `git status`·`git diff-tree HEAD`로 변경을 감지한다.
> **이 폴더는 아직 git 저장소가 아니라** 두 명령이 실패해 stdout이 비고,
> 정규식(`/.(mjs|json)$/m`)이 안 맞아 `exit 0`으로 **조용히 통과한다.**
> 넣어두고 도는 줄 알지만 한 번도 안 도는 상태가 된다 — **`git init` + 첫 커밋이 선행 조건**이며,
> 이를 ④ **Phase 0**으로 못박았다.

### 버전 보관 방식 — 덮어쓰지 않고 개별 보관

**시나리오 (사용자 원문 기준):**

1. 담당자 **A**가 코드 `aaa v1`을 작성해 등록한다
2. 수정 요청이 오지만 A가 부재·대응 불가 → 담당자 **B**가 `aaa v1`을 받아 `aaa v2`로 수정해 등록한다
3. **`v1`과 `v2`가 모두 대시보드/허브에서 확인 가능하다**

**규칙:**

- 새 버전을 등록해도 **기존 버전을 덮어쓰지 않는다.** 버전별로 개별 보관한다.
- 각 버전에는 **누가 작업했는지**가 남는다 (`v1` → A, `v2` → B).

**개별 보관을 하는 목적:**

- 이력 추적·인수인계뿐 아니라, **담당자별로 진행한 업무 코드 개수를 파악**하기 위해서다.
  덮어쓰면 B의 `v2`만 남아 A가 `v1`을 만든 사실이 사라진다.

> 이 요구로 **대시보드/허브가 "버전을 확인하는 화면"** 역할을 갖게 된다.
> PLAN.md ③에서 대시보드는 「되면 좋은」에 있다 — 조정이 필요한지 확인할 것.

### 범위 조정 — 대화 중 확정된 것

| 항목 | 결정 | 비고 |
|---|---|---|
| 프로젝트명 | **`workflow-hub` 유지** | 상징어 후보(Hermes·Ariadne 등)를 검토했으나 현행 유지 |
| 대시보드 | **되면 좋은** | 형태는 **정적 HTML 리포트** — 명령 한 번으로 `index.html` 생성, 서버·빌드 없음 |
| LLM wiki | **되면 좋은** | REQUEST 최종 목표 중 하나이나 이번 6~8시간에서는 제외 |
| 담당자별 업무 코드 개수 통계 | **현재 scope 제외 — 이후 구현** | 버전별 개별 보관을 해두면 나중에 셀 수 있다. 집계 기능 자체는 미룸 |
| 백로그(요청 리스트 관리) | **제품 기능에서 제외** | **구현 과정에서 Phase 추적 도구로만** 사용. 완성 후 관리 단위는 버전별 개별 저장 |
| `version-history-guard` hook | **Phase 2에 병합** | 독립 Phase로 두지 않음 |
| backlog MCP | **day2 구현 코드를 그대로 복사해 쓰되 서버는 독립 등록** | day2 파일은 수정하지 않음 |
| 승인 지점 | **매 Phase 종료 시 1회만** | Codex 반박을 통과했을 때만 사람에게 올라옴 |

### GitHub 저장소

```
origin  https://github.com/nampluskr/workflow-hub.git
```

- 저장소는 **이미 생성돼 있다.**
- Phase 0에서 `git init` + 첫 커밋 후 `git remote add origin`으로 연결한다.

### 문서 체계 — 다섯 종류 (미확정, 1:1 코칭에서 확정)

사용자가 요구한 문서 종류다. **현재 계약(PLAN.md)에는 반영돼 있지 않다.**

1. **계획서**
2. **요청처** — 요청 출처·요청자 정보
3. **AI 해석 내용** — 에이전트가 코드를 읽고 정리한 것
4. **매뉴얼** — 사용 설명
5. **결과 보고서**

> ⚠️ **계약과의 불일치**
> PLAN.md ③의 「반드시」는 샘플을 *"코드 + `MANUAL.md`"* 로만 잡고 있고,
> ④ Phase 1의 완료 조건도 그 둘만 센다. 다섯으로 늘리면:
> - Phase 1의 샘플 2개가 **문서 10장**이 된다
> - `CLAUDE.md` 작성 규칙이 다섯 배로 커진다
> - Phase 3 브리핑이 읽어야 할 대상도 늘어난다
>
> **코칭 전에 구현을 시작하면 Claude는 `MANUAL.md` 하나만 있는 현재 계약대로 만든다.**

### 툴 · 언어 (미확정, 1:1 코칭에서 확정)

- 구현 언어 — day1·day2가 Node(`.mjs`)인데 그대로 갈지
- 부서 실제 업무 코드의 언어와 달라도 되는지 — 브리핑 서브에이전트가 그 언어를 읽어야 한다
- 외부 패키지 사용 가능 여부 — day2는 SPEC에서 금지했었다

### 버전 관리 방법 (미확정, 1:1 코칭에서 확정)

- 개별 보관의 **디렉터리·명명 규칙** (`aaa/v1/`, `aaa/v2/` 형태?)
- **git 태그·브랜치로 할지, 파일 디렉터리로 할지**
  - ⚠️ 이게 정해지면 **Phase 2 전체가 바뀐다.** git 태그로 가면 이력 파일 SSOT와
    `version-history-guard` hook이 통째로 불필요해질 수 있다 — 「반드시」의 절반이 걸린 질문이다
- 코드와 매뉴얼의 버전을 묶어서 올리는지, 따로 올리는지
- 버전이 늘었을 때 브리핑이 어디까지 읽어야 하는지 (최근 N건?)
