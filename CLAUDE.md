# workflow-hub 작성 규칙

부서원 4명이 **원 담당자가 아닌 사람도** 코드·매뉴얼·버전 이력을 같은 형식으로 찾을 수 있게 하기 위한 규칙이다.
이 규칙을 따르지 않으면 버전 네비게이션 서브에이전트(Phase 3)가 필요한 정보를 못 찾는다.

## 1. 프로젝트 디렉터리 규칙

담당 업무 하나 = `samples/<프로젝트명>/` 폴더 하나. 프로젝트명은 영문 소문자·하이픈만 쓴다.

```
samples/<프로젝트명>/
  README.md           필수 — 아래 "2. README.md 형식"을 따른다
  src/
    main.py            필수 — 실행 진입점. 이 파일명·경로는 고정이다 (검사 도구가 이 경로만 본다)
  docs/
    report.md           필수 — 아래 "3. docs/report.md 형식"을 따른다
  data/ 또는 images/  선택 — 입력 데이터. main.py가 기본으로 읽는 위치
  NAVIGATION.md        선택 — 버전 등록 직전 버전 네비게이션 서브에이전트(Phase 3)가 생성. 아래 "6. NAVIGATION.md 형식" 참고
  (그 외 보조 모듈은 src/ 안에 자유 배치, 단 README.md의 "수정 시 참고"에 반드시 나열)
```

- 각 샘플은 **최종적으로 개별 GitHub 레포가 된다**는 전제로 이 구조를 쓴다: 레포 루트의 `README.md`는 GitHub이 자동으로 렌더링해서 보여주고, `src/`를 열면 코드가 바로 보인다 — 그래서 대시보드에 "매뉴얼"·"코드" 링크를 따로 두지 않는다(자동 렌더링 안 되는 `docs/report.md`만 링크가 필요).
- `README.md`, `src/main.py`, `docs/report.md`는 **반드시 이 경로**에 있어야 한다. 임의로 옮기지 않는다.
- 실행 언어는 프로젝트마다 달라도 된다(부서 실무 코드가 실제로 그렇다). 다만 진입점 파일명은 언어에 맞는 관례를 따르되(`main.py`, `main.js` 등) **`src/main.<확장자>` 형태를 유지**한다.
- `docs/report.md`는 2026-08-13에 추가된 요구사항이다(당시 이름은 `CODE_ANALYSIS.md`). 그 이전에 등록된 버전 스냅샷(예: 이 문서 기준 `v1.0`/`v2.0`)에는 없을 수 있다 — 소급 적용하지 않는다. `README.md`/`src/`/`docs/` 구조 자체도 같은 날 도입됐다 — 그 이전 스냅샷은 루트에 `main.py`/`MANUAL.md`가 있는 옛 구조 그대로다.

## 2. README.md 형식

아래 네 섹션을 **이 제목 그대로**, 이 순서로 포함한다. **사용법 문서다** — "어떻게 쓰는가"만 다루고 구현 이유는 다루지 않는다.

```markdown
## 용도
이 프로그램이 부서 업무에서 어떤 문제를 푸는지, 한 문단.

## 입출력
입력 파일(형식·위치)과 출력(파일·화면)을 각각 적는다.

## 실행 방법
그대로 복사해서 돌릴 수 있는 명령어.

## 수정 시 참고
수정 요청이 들어왔을 때 건드릴 파일과 각 파일의 역할. 파일마다 한 줄.
```

## 3. docs/report.md 형식

README.md가 "어떻게 쓰는가"라면 이건 **"왜 이렇게 짰는가"**다 — 코드 작성 담당자가 자기 구현을 설명하는 문서. 버전 네비게이션 서브에이전트(`report-history-analyzer`)가 "핵심 로직 설명" 절을 그대로 인덱스의 근거로 삼으므로, 실제로 이해를 돕는 밀도로 쓴다.

아래 네 섹션을 **이 제목 그대로**, 이 순서로 포함한다.

```markdown
## 구현 개요
전체 구조를 텍스트로 설명한다 — 파일이 어떤 역할로 나뉘는지, 데이터가 어떻게 흐르는지.

## 핵심 로직 설명
가장 중요한 함수·로직을 다룬다. 필요하면 그 함수의 코드를 발췌해도 된다 —
오히려 핵심 함수는 발췌하는 편이 낫다. 다만 **발췌마다 "왜 이렇게
구현했는지"를 텍스트로 반드시 설명한다.** 코드에 주석만 달아놓고 설명 없이
그대로 옮겨 적지 않는다 — 주석은 코드가 "무엇을" 하는지고, 여기 필요한 건
"왜" 그렇게 했는지다.

## 이번 버전에서 바뀐 점과 이유
이 버전을 등록하며 무엇을 왜 바꿨는지. 최초 버전이면 "왜 이렇게 처음
설계했는지".

## 알려진 제약 · 다음에 볼 사람이 알아야 할 것
트레이드오프, 잠재 이슈, 확장 시 주의점.
```

## 4. 버전 관리 규칙 (Phase 2에서 도구화)

- 버전 이력의 SSOT는 `.claude/version-history/<프로젝트명>/history.json`이다. **직접 `Edit`/`Write`로 고치는 것을 금지**한다 (`version-history-guard` hook이 막는다) — 반드시 버전 등록 스킬을 통해서만 쓴다.
- 새 버전을 등록하면 그 시점의 `samples/<프로젝트명>/` 스냅샷이 `.claude/version-history/<프로젝트명>/v<버전>/`에 **개별 보관**된다. 이전 버전 스냅샷은 덮어쓰지 않는다.
- 각 버전 항목에는 최소한 **작업자·날짜·변경 요약**을 남긴다. 작업자는 실명 또는 사번 등 부서에서 통일한 식별자를 쓴다.
- **버전 번호는 `메이저.마이너` 형식**(`v1.0`, `v1.1`, `v2.0` ...)이고, `tools/version.mjs register`가 직전 등록 항목의 작업자와 비교해 자동으로 정한다 — 사람이 번호를 직접 고르지 않는다.
  - **직전 버전과 담당자가 같으면 마이너 증가**(`v1.0` → `v1.1`): 같은 사람이 이어서 손댄 것이므로 "그 담당자 안에서의 개정"으로 본다.
  - **직전 버전과 담당자가 다르면 메이저 증가, 마이너는 0으로 초기화**(`v1.x` → `v2.0`): 담당자가 바뀌었다는 것 자체가 인수인계 브리핑이 주목해야 할 지점이라, 버전 번호에서부터 눈에 띄게 한다.
  - 프로젝트의 최초 등록은 담당자와 무관하게 항상 `v1.0`이다.

## 5. 요청 문서 (선택, 요청이 있었던 경우)

수정 요청을 받아 작업했다면 `samples/<프로젝트명>/REQUEST.md`에 요청자·요청일·요청 내용을 남긴다. 없으면 생략 가능 — 최초 작성 시에는 요청 문서가 없는 것이 정상이다.

## 6. NAVIGATION.md 형식 (Phase 3, 서브에이전트가 생성)

사람이 직접 쓰는 문서가 아니다. **"이 기능/주제가 어느 버전 report.md 절에 설명돼 있고, 지금 코드의 어디에 있는지"를 연결하는 네비게이션 인덱스**다 — 요약·설명 문서가 아니라 찾아가는 지도다. 나중에 만들 LLM wiki(대화형 Q&A 서브에이전트)가 report.md·코드 전체를 다시 읽지 않고 이 표만 보고 정확한 위치로 점프하기 위한 기반 자료이므로, 각 행은 구체적인 절 제목·줄 번호까지 가리켜야 한다.

```markdown
# <프로젝트명> — 네비게이션 인덱스

| 기능/주제 | 설명된 버전 (report.md 절) | 현재 코드 위치 |
|---|---|---|
| aggregate | v1.0 report.md "핵심 로직 설명" | src/main.py:12 |
```

**생성 주체와 시점**: 세 서브에이전트로 나뉜다 — `.claude/agents/code-analyzer.md`(코드에서 함수 위치를 찾음), `.claude/agents/report-history-analyzer.md`(현재+과거 모든 버전 스냅샷의 report.md에서 어느 절에 설명됐는지 찾음), `.claude/agents/version-navigator.md`(위 둘을 호출해 결과를 받아 표로 조인하고 파일을 쓰는 오케스트레이터). **`register-version`으로 새 버전을 등록하기 직전에 `version-navigator`를 호출**한다 — `register-version` 스킬 절차에 이 단계가 포함돼 있다.

- report.md의 "핵심 로직 설명"에 없는 함수는 행에 넣지 않는다(코드 전체 커버리지가 목적이 아니라 "이미 문서화된 것을 찾아가는" 목적).
- `samples/<프로젝트명>/` 안에 있으므로, 버전 등록(§4) 시점의 스냅샷에 그때 상태 그대로 같이 얼어붙는다. 등록 **전**에 만들어야 그 버전 스냅샷에 포함된다 — 등록 후에 만들면 다음 버전에서야 반영된다.
